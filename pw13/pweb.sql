create database pweb;
use pweb;
select * from clientes;
select * from produtos;
-- cria apenas o banco
-- orm: arquitetura onde o próprio software de orm gerencia as tabelas de um banco de dados relacionados
-- baseados na estrutura de objetos e classes.